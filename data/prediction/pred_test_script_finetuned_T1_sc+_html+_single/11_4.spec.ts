import { test, expect } from '@playwright/test';
import { writeFileSync } from 'fs';
test('test', async ({ page }) => {
  await page.goto('http://localhost:8080/cadenza/');
  await page.getByRole('link', { name: 'Anmelden' }).click();
  await page.getByLabel('Benutzername *').click();
  await page.getByLabel('Benutzername *').fill('Admin');
  await page.getByLabel('Benutzername *').press('Tab');
  await page.getByPlaceholder(' ').fill('Admin');
  await page.getByRole('button', { name: 'Anmelden' }).click();
  await page.getByTestId('create-workbook-button').click();
  await page.getByRole('menuitem', { name: 'Gewässer' }).click();
  await page.getByRole('button', { name: 'Arbeitsblatt' }).click();
  await page.getByRole('menuitem', { name: 'Neues Arbeitsblatt' }).click();
  await page.getByTestId('name-input').getByLabel('Name des Arbeitsblatts').click();
  await page.getByTestId('name-input').getByLabel('Name des Arbeitsblatts').fill('Blatt');
  await page.getByTestId('name-input').getByLabel('Name des Arbeitsblatts').press('Enter');
  await page.getByTestId('add-button').click();
  await page.getByTestId('add-button').getByLabel('Hinzufügen').click();
});