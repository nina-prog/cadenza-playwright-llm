import { test, expect } from '@playwright/test';
import { writeFileSync } from 'fs';
test('test', async ({ page }) => {
  await page.goto('http://localhost:8080/cadenza/');
  await page.getByRole('link', { name: 'Anmelden' }).click();
  await page.getByLabel('Benutzername *').click();
  await page.getByLabel('Benutzername *').fill('Admin');
  await page.getByLabel('Benutzername *').press('Tab');
  await page.getByPlaceholder(' ').fill('Admin');
  await page.getByRole('button', { name: 'Anmelden' }).click();
  await page.getByText('Verzeichnis Gewässergüte', { exact: true }).click();
  await page.getByRole('link', { name: 'Übersicht Messstellen' }).click();
  await page.getByText('v12', { exact: true }).click();
  await page.getByText('Informationen zur Arbeitsmappe', { exact: true }).click();
  await page.getByText('Speichern unter …', { exact: true }).click();
  await page.getByText('Speichern', { exact: true }).click();
  await page.getByText('Speichern und veröffentlichen', { exact: true }).click();
  await page.getByText('Zugriffseinstellungen', { exact: true }).click();
  await page.getByText('Neue Sicht', { exact: true }).click();
  await page.getByText('Mehr', { exact: true }).click();
  await page.getByText('Daten laden', { exact: true }).click();
  await page.getByText('Weitere Daten hinzufügen', { exact: true }).click();
});